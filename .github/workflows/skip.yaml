name: example-workflow skip
on: [push]
jobs:
  production-deploy:
    if: github.repository == 'mouli429/GH-200'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '14'
      - run: npm install -g bats
